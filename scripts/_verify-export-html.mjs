// Scratch file created during a Claude session to render an HTML preview of
// a booklet for verification (Chrome/puppeteer was unavailable in that
// sandbox, so WeasyPrint was used instead for a quick look).
// Safe to delete — not part of the build pipeline, not referenced anywhere.
